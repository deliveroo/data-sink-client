require "spec_helper"

describe Data::Sink::Client do
  it "has a version number" do
    expect(Data::Sink::Client::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
